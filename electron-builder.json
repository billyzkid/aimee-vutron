{
  "appId": "com.github.billyzkid.aimee-vutron",
  "artifactName": "${name}-setup-${version}-${os}-${arch}.${ext}",
  "files": ["dist", "!dist/builds"],
  "directories": {
    "buildResources": "resources/build",
    "output": "dist/builds"
  }
}
